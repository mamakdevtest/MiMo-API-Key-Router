# Required only while creating a new database; remove after first successful start.
INITIAL_ADMIN_PASSWORD=replace-with-a-strong-password

# Use a stable, secret gateway key for clients. It is stored as a hash on first boot.
GATEWAY_KEY=replace-with-a-long-random-gateway-key

# Runtime
NODE_ENV=production
HOST=0.0.0.0
PORT=4000
APP_NAME=AI Provider Router
LOG_LEVEL=info

# Local SQLite database. Docker Compose overrides this to its persistent /data volume.
DATABASE_URL=file:./data/api-router.sqlite

# GATEWAY_KEY must be at least 32 characters and remain stable. The router
# derives its credential-encryption key from it; no extra encryption or
# session-signing secret is required.

# Reverse-proxy and session settings.
TRUST_PROXY=true
COOKIE_SECURE=true
SESSION_MAX_AGE_SECONDS=86400

# Maximum upstream attempts for one gateway request. The default supports
# Vercel mix routes that try active keys twice for multiple target models.
MAX_UPSTREAM_ATTEMPTS=120

# Leave false unless providers are intentionally hosted on a private network.
ALLOW_PRIVATE_PROVIDER_URLS=false
